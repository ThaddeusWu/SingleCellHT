% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_scht.R
\name{create_scht}
\alias{create_scht}
\title{Create a Single-Cell Hierarchical Tensor (SCHT) from raw count data}
\usage{
create_scht(
  gene_counts,
  transcript_counts,
  transcript_info,
  cell_info,
  n_hvg = 1000,
  qc_params = list(min_genes_per_cell = 200, max_genes_per_cell = 10000,
    min_cells_expressing = 0.02, min_expr = 1e-06),
  require_stage = FALSE,
  verbose = TRUE,
  enable_census = TRUE
)
}
\arguments{
\item{gene_counts}{A matrix or data frame of gene-level raw counts}

\item{transcript_counts}{A matrix or data frame of transcript-level raw counts}

\item{transcript_info}{A data frame describing transcripts}

\item{cell_info}{A data frame describing cells}

\item{n_hvg}{Number of highly variable genes (default: 1000)}

\item{qc_params}{List of quality control parameters:
\itemize{
\item min_genes_per_cell Minimum genes per cell
\item max_genes_per_cell Maximum genes per cell
\item min_cells_expressing Minimum fraction of cells expressing a gene
\item min_expr Minimum expression threshold
}}

\item{require_stage}{Logical, whether stage information is required}

\item{verbose}{Logical, whether to show progress messages}

\item{enable_census}{Logical, whether to perform census normalization}
}
\value{
A SCHT object
}
\description{
This function constructs a SCHT object from raw gene- and transcript-level
count matrices.
}
\examples{
# Suppose you have raw count matrices 'gene_counts' and 'transcript_counts',
# along with 'transcript_info' and 'cell_info'. 
#
# scht_obj <- create_scht(
#   gene_counts = gene_counts,
#   transcript_counts = transcript_counts,
#   transcript_info = transcript_info,
#   cell_info = cell_info,
#   n_hvg = 1000,
#   min_genes_per_cell = 10,
#   min_cells_per_gene = 0.05,
#   min_expr = 1e-6,
#   require_stage = FALSE,
#   verbose = TRUE,
#   enable_census = TRUE
# )
#
# print(scht_obj)
# summary(scht_obj)
}
